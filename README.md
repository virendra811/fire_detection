# fire&smoke

This project is an image-based fire detection system that analyzes uploaded images to determine whether fire is present. The model processes the image and returns the detection result along with a confidence score indicating the probability that the image contains fire.

When fire is detected, the system highlights the affected area by drawing a bounding box around the detected region and displays the confidence percentage of the prediction. This helps users quickly identify the location of the fire within the image.
